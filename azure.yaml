# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: pycascades23-website
metadata:
  template: pycascades23-website@0.0.1-beta
services:
  web:
    project: .
    language: py
    host: appservice
